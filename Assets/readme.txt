Note the below dependencies are from licensed plugins and are thus not included in the repo.
If you have these plugins install them through the package manager.
If you do not own these plugins you will run into compile errors. Fix this by removing all non native attributes from the scripts. (Example: [Command])

Dependencies:
Plugins-QFSW:Quantum Console.
Plugins-Sirenix: Odin Inspector.
